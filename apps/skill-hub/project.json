{
  "name": "skill-hub",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/skill-hub",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project skill-hub --web",
  "targets": {
    "deploy-prod": {
      "executor": "nx:run-commands",
      "options": { "command": "sh deploy.sh prod apps/skill-hub" }
    }
  }
}

import { Card, CardContent } from '@intern-3a/shadcn';
import React from 'react';

export const ReferPageCandidateDetail = () => {
  return (
    <Card>
      <CardContent>ReferPageCandidateDetail</CardContent>
    </Card>
  );
};

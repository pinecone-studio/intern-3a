import { Card, CardContent } from '@intern-3a/shadcn';
import React from 'react';

export const ReferPageEmployeeDetail = () => {
  return (
    <Card>
      <CardContent>ReferPageEmployeeDetail</CardContent>
    </Card>
  );
};
